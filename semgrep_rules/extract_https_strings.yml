rules:
  - id: extract-https-strings
    patterns:
      - pattern: |
          "https://$URL"
    message: "Found an HTTPS URL"
    severity: INFO

  - id: extract-http-strings
    patterns:
      - pattern: |
          "http://$URL"
    message: "Found an HTTP URL"
    severity: INFO
